---
id: container
term: 容器
en: Container
aliases:
  - 容器
  - Container
  - container
  - 容器技术
definition: 基于操作系统内核的 Namespace（命名空间视图隔离）与 Cgroups（物理资源配额限制）技术构建的轻量级、自包含应用运行实例。与虚拟机不同，容器直接共享宿主机操作系统内核，具备秒级启动、极低资源开销与环境一致性优势。
enabled: true
---

容器是现代云原生计算的核心基石与标准交付物。它将应用代码、依赖库与操作系统文件系统打包为不可变的镜像，实现“一次构建，随处运行”，彻底解决了开发与生产环境不一致的跨机迁移难题。
