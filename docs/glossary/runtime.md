---
id: runtime
term: 运行时
en: Runtime
aliases:
  - Runtime
definition: 程序运行所依赖的解释器或虚拟机、语言运行时与系统库等执行环境的总称，决定“这段代码能不能在这台机器上跑起来”。
enabled: true
---

以 Java 为例需要 JVM，以 Python 为例需要对应版本的解释器与依赖库。构建决定“产出什么”，运行时决定“在哪能跑”——两者割裂是跨环境部署最常见的坑。
